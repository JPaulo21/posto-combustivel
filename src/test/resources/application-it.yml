spring:
  application:
    name: posto-combustivel
  datasource:
    url: jdbc:h2:mem:db:abastecimentos;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2

server:
  error:
    include-stacktrace: never
  servlet:
    context-path: /posto-combustivel